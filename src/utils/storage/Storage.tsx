// import * as SecureStore from 'expo-secure-store';

// export const StoreLocalAdd = async (key: string, value: any) => {
//   const res = await SecureStore.setItemAsync(key, value);
//   return res;
// };

// export const StoreLocalGet = async (key: string) => {
//   const res = await SecureStore.getItemAsync(key);
//   return res;
// };

// export const StoreLocalRemove = async (key: string) => {
//   const res = await SecureStore.deleteItemAsync(key);
//   return res;
// };
